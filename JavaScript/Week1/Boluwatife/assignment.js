let name = "John"

let admin = name;
name = "John";
admin = name;

console.log(admin); //John

let ourPlanetNamee = "Earth";

let bets = "crown:0, anchor:0, heart:0, spade:0, club:0, diamond:0";
let totalBets = ("totalBet = rand(1, funds)")
{
    if(totalBet === '7?'){
    totalBet = "funds";
    }else{
     totalBet = "randomly distribte totalBet";
    }else if(totalBet = "funds"){
     bets("heart") = totalBet{
     }else{
        "randomly distribte totalBet";
     }
    }
    
}

let num;

do {
  num = prompt("Enter a number greater than 100?", 0);
} while (num <= 100 && num);
"randomly distribte totalBet"
